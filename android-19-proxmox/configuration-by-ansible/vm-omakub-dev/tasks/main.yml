---
# Omakub Dev VM Installation (Automated Ubuntu + Omakub via cloud-init)
#
# This provides fully automated installation using Ubuntu cloud image and cloud-init
# for headless Ubuntu + Omakub setup without manual intervention.

- name: Include cloud image management tasks
  include_tasks: cloud-image-management.yml
  tags: [cloud-init, download]

- name: Include cloud-init configuration tasks
  include_tasks: cloud-init-setup.yml
  tags: [cloud-init, configure]

- name: Include post-installation verification
  include_tasks: post-install.yml
  tags: [post-install, verify]